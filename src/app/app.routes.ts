import { Routes } from '@angular/router';
import { TemplateEditorComponent } from './dashboard/components/template-editor/template-editor.component';

export const routes: Routes = [
    {path: '', component:TemplateEditorComponent}
];
